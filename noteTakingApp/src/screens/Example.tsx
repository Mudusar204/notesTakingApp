import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const Example = () => {
  return (
    <View>
      <Text style={{color: 'black'}}>Example</Text>
    </View>
  );
};

export default Example;

const styles = StyleSheet.create({});
